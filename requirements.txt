# Core dependencies — installed with: pip install -r requirements.txt
PyMuPDF>=1.24
PyYAML>=6.0

# Optional: QR/barcode detection inside page images.
# Requires the zbar system library first:  brew install zbar
# then:  pip install pyzbar pillow
