# Python dependencies — installed automatically by scripts/run.sh
# (or manually with: .venv/bin/pip install -r requirements.txt)
# Runtime: Python 3.13 (Homebrew python@3.13; run.sh bootstraps it)
PyMuPDF>=1.24
PyYAML>=6.0

# QR/barcode detection inside page images (self-contained wheel).
zxing-cpp>=2.0
pillow>=10.0

# Office document support (pure Python)
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1

# Extra image formats
pillow-heif>=0.16      # .heic / .heif
rawpy>=0.24            # camera RAW decode (.cr2/.cr3/.nef/.arw/.dng) — decode only

# Email attachment support (.msg) — pure Python
extract-msg>=0.45

# Encrypted .docx/.xlsx/.pptx support — pure Python
msoffcrypto-tool>=5.4  # encrypt()/decrypt() round-trip spiked on 6.0

# Handwriting OCR + face detection (options: handwriting_ocr /
# redact_handwriting / redact_faces — all default OFF). Apple's on-device
# Vision framework; fully local, no network.
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0

# Used only by fixture/sample generators
qrcode>=7.0
